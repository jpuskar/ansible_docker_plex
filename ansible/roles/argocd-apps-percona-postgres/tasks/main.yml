---
- name: Template out namespace manifest
  template:
    src: namespace.yaml
    dest: "{{ output_dir_path }}/namespace.yaml"

- name: Include percona-postgres tasks
  include_tasks: percona-postgres.yml
