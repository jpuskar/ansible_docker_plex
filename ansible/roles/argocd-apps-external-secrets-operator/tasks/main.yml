---
- name: Template out namespace manifest
  template:
    src: namespace.yaml
    dest: "{{ output_dir_path }}/namespace.yaml"

- name: Include external-secrets-operator tasks
  include_tasks: external-secrets-operator.yml
