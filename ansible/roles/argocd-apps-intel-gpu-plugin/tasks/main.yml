---
- name: Template out namespace manifest
  template:
    src: namespace.yaml
    dest: "{{ output_dir_path }}/namespace.yaml"

- name: Template out Intel GPU Device Plugin DaemonSet
  template:
    src: intel-gpu-plugin-daemonset.yaml
    dest: "{{ output_dir_path }}/intel-gpu-plugin-daemonset.yaml"
