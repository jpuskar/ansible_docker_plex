---
- name: Template out namespace manifest
  template:
    src: namespace.yaml
    dest: "{{ output_dir_path }}/namespace.yaml"

- name: Include nvidia-gpu-operator tasks
  include_tasks: nvidia-gpu-operator.yml
