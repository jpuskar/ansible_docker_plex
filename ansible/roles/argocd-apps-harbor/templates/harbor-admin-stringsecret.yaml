---
apiVersion: "secretgenerator.mittwald.de/v1alpha1"
kind: "StringSecret"
metadata:
  name: harbor-admin-secret
  namespace: {{ harbor_namespace }}
spec:
  forceRegenerate: false
  fields:
    - fieldName: "HARBOR_ADMIN_PASSWORD"
      encoding: "base64"
      length: "32"
