---
apiVersion: cilium.io/v2alpha1
kind: CiliumLoadBalancerIPPool
metadata:
  name: lb-pool
spec:
  blocks:
    - start: {{ cilium_lb_pool_start }}
      stop: {{ cilium_lb_pool_stop }}
    - cidr: {{ plex_load_balancer_ip }}/32
---
apiVersion: cilium.io/v2alpha1
kind: CiliumL2AnnouncementPolicy
metadata:
  name: l2announce
spec:
  loadBalancerIPs: true
