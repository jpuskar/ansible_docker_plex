---
- hosts: localhost
  vars:
    cluster_name: k8s2
  tasks:
    - name: include tasks/1_set_vars.yml
      include_tasks: ../tasks/1_set_vars.yml

    - name: include metallb role
      include_role:
        name: metallb
